# Copilot Instructions

- Prefer **JavaScript (ES2020)** and **Python 3.10**
- Use functional React components and TailwindCSS
- Reference `news/ingest.js` for ingestion patterns
- Run `npm run lint`, `npm test`, and `pytest` before committing
- Follow Conventional Commit messages
- Avoid hardcoding secrets; read from environment variables
